import { ToEurDirectiveDirective } from './to-eur-directive.directive';

describe('ToEurDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ToEurDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
