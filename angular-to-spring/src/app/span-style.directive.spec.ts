import { SpanStyleDirective } from './span-style.directive';

describe('SpanStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SpanStyleDirective();
    expect(directive).toBeTruthy();
  });
});
