import { CcFontWeightDirective } from './page2/cc-font-weight.directive';

describe('CcFontWeightDirective', () => {
  it('should create an instance', () => {
    const directive = new CcFontWeightDirective();
    expect(directive).toBeTruthy();
  });
});
