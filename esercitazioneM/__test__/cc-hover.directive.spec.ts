import { CcHoverDirective } from '../src/app/cc-hover.directive';

describe('CcHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CcHoverDirective();
    expect(directive).toBeTruthy();
  });
});
